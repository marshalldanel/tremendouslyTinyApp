<!DOCTYPE html>
<html lang = 'en'>
<head>
    <title>TinyApp</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <% include partials/_header %>
</head>
<body>
  <h3>Here's your tremendously tiny URL!</h3>
  <table>
    <tr>
      <th><%=`Long: ${longURL}`%></th>
      <th><%=`Tiny: ${shortURL}`%></th>
    </tr>
  </table>
  <form method='POST' action='/urls/<%= shortURL %>'>
    <input id='longURL' type='text' name = 'longURL' placeholder='http://'>
    <input type='submit' value='Update'>
  </form>
  <a href="/urls/">Take me to my TinyURLs!</a>
</body>
</html>