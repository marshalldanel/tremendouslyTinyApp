<!DOCTYPE html>
<html lang = 'en'>
<head>
    <title>TinyApp</title>
    <meta charset="UTF-8">
    <% include partials/_header %>
</head>
<body>
  <a href = '/urls/new'>Make your URL super duper tiny!</a>
  <table>
    <tr>
      <th>Long URL</th>
      <th>Tiny URL</th>
    </tr>
    <% for(let db in urls) { %>
      <tr>
        <td><%= urls[db] %></td>
        <td><%= db %></td>
        <td><a href='/u/<%=db%>'>My tiny link</a></td>
        <td><a href="/urls/<%=db%>">Edit</a></td>
        <td>
          <form method='POST' action='/urls/<%= db %>/delete'>
            <input type='submit' value='Delete'>
          </form>
        </td>
      </tr>
    <% } %>
  </table>
</body>
</html>